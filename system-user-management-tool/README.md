Managing users on Linux manually can be repetitive and error-prone.  
This automation tool reads a list of users from a CSV file, performs the required user management operations (create, update, role assignment), logs all actions, and then generates an analytical report.
